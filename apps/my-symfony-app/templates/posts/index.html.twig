{% extends 'base.html.twig' %}
{% block title %} blog{% endblock %}
{% block body %}
    <h1>posts</h1>

    <form class="form-inline my-2 my-lg-0" action="{{ path('blog_search') }}" method="get">
        <input class="form-control mr-sm-2" placeholder="Поиск" type="search" aria-label="Search" name="q">
        <button class="btn btn-default my-2 my-sm-0" type="submit">Искать</button>
    </form>
<br>
    {% for post in posts %}
        <p> <a href="{{ path('one_post', {'slug': post.slug}) }}">{{ post.title }}</a></p>
    {% endfor %}




{% endblock %}
